public class Duck implements Comparable {
  String name;
  int weight;
  public Duck(String name, int weight) {
    this.name = name;
    this.weight = weight;
  }
  public String toString() {
    // TODO: define the string representation of this object
  }
  public int compareTo(Object object) {
    // TODO:
    //  return -1 if this object is less than the passing object
    //  return 0 if they are equal
    //  return 1 if this object is the greater.
  }
}